# Front-end Applications for Soap APIs

##Naming Convention
```apps/[name-of-app]```

###Scripts
####Run App:
```
./build.ps1 <relative app path e.g. ./soap.app.sample>
```

####Build App:
```
./build.ps1 <relative app path e.g. ./soap.app.sample> build
```